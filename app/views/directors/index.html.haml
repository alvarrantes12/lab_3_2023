%p{:style => "color: green"}= notice
.div.m-4
  .page.header
    %h1.mb-4
      = t('application.director_list')
    .float-end
      = link_to t('application.create'), new_director_path, class: "btn btn-primary"

  %table.table.table-striped
    %thread
      %tr
        %th= t('activerecord.attributes.director.first_name')
        %th= t('activerecord.attributes.director.last_name')
        %th
        %th
    %tbody
      - @directors.each do |director|
        %tr
          %td= director.first_name
          %td= director.last_name
          %td= link_to t('application.show'), director, class: "btn btn-info"
          %th= link_to t('application.edit'), edit_director_path(director), class: "btn btn-success"
          %th= button_to t('application.delete'), director, method: :delete, class: "btn btn-danger"
